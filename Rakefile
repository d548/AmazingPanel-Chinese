# 把自己的任务添加进文件。文件位于lib/tasks，以.rake结尾。  Add your own tasks in files placed in lib/tasks ending in .rake,
# 例如，lib/tasks/capistrano.rake，they will automatically be available to Rake  
# for example lib/tasks/capistrano.rake, and they will automatically be available to Rake.
require File.expand_path('../config/application', __FILE__)
require 'rake'
require 'single_test'


AmazingPanel::Application.load_tasks
SingleTest.load_tasks
